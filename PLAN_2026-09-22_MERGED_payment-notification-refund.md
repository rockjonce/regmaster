# PLAN 2026-09-22 — 合併修正案：繳費通知誤導 ＋ 退費統計口徑

> **狀態**：兩份計畫已合併分析、程式碼已逐條驗證。**本合併案本體尚未實作、未部署。**
> ⚠️ **但基準已變動**：2026-09-23 另有一批折扣碼修正已獨立完成並上線 production（見 §0）。
> **原碼基準**：原為 `18f641f`；**現為 `f2b21ce`**。兩份來源計畫的行號均對應 `18f641f`，
> 施工前必須以「函式名稱 + 行號」雙重定位重新核對，不可只依行號。
> **來源計畫**：
> - A 案 `PLAN_2026-09-09_unpaid-registration-ux.md`（繳費通知誤導，五輪複掃）
> - B 案 `PLAN_2026-09-22_refund-stats-consistency.md`（退費統計口徑，v2 裁決版）
>
> **「一次修正」的定義**：本文件是**一份完整的修正案**（範圍一次定義完、不再追加），
> 但**部署仍分批**。理由見 §6——合併後有兩處改動是不可逆的（寄信、退刷），
> 一次全佈會讓這些不可逆動作失去驗收窗口。

---

## 0. 基準變動：2026-09-23 折扣碼修正已獨立上線（commit `f2b21ce`）

本合併案**尚未開工**，但期間另有一批緊急修正已完成並佈署 production。它不屬於本案範圍，
但**改到了本案預計要動的四個檔案**，因此施工前必須知道。

### 0.1 那批做了什麼

報名頁原本沒有折扣碼輸入欄，且送出時把 `createRegistrationPayment` 的第三個參數寫死為
空字串，導致走「報名 → 直接刷卡」主流程的報名者根本用不到折扣碼。已補上輸入欄、
報價預覽，並新增活動層級的啟用開關（`cfg.discountCodeEnabled`，預設關閉）。

### 0.2 與本合併案的重疊 ⚠️

| 檔案 | 0923 已改 | 本案預計改 | 施工前必須做的事 |
|---|---|---|---|
| `functions/index.js` `jk` 白名單 | 加 `discountCodeEnabled`（陣列尾端換行） | **B1** 加 `paymentDeadline`、`paymentPolicyFrom` | 行號已位移；照函式名重新定位。追加到同一陣列即可，互不衝突 |
| `public/events/register.html` | 付款步驟新增折扣碼區塊（+101 行）、`STATE` 新增欄位、送出段帶入折扣碼 | **C1/C2**（批 5）繳費規則條款與成功頁文案 | **行號大幅位移**。C1 的插入點與折扣碼區塊相鄰，需重新定位 |
| `public/events/payment.html` | 折扣碼區塊改為依開關顯示 | **F14**（批 5）已退費隱藏付款方式 | 兩者位置不同，無衝突 |
| `public/shared/firebase-bridge.js` | `validateDiscountCode` **尾端追加** `peopleCount` | **F17**（批 6）`ownerRefund` 追加 `cancelRegistration` | 不同端點、互不影響。**但已確立先例：尾端追加是安全模式**（舊呼叫端少傳的參數為 undefined，JSON 序列化時被丟棄，後端 fallback） |

### 0.3 對本案風險評估的影響

| 項目 | 影響 |
|---|---|
| §6.1 三大高風險項 | **不變**。0923 那批沒有碰 `submitRegistration`、`createRegistrationPayment` 的閘門、或任何寄信路徑 |
| §3.3 F11 `createRegistrationPayment` | **仍缺付款狀態閘門**。0923 只是讓它收得到折扣碼參數，沒有加閘門。F11 照原計畫執行 |
| §6.2 「位置參數錯位」風險 | **降低**。已有一次尾端追加的成功實例可循，F17 可直接沿用同一模式 |
| 手續費計算 | 已查證：`regPayments.amount` 存的即為折後金額，平台手續費以實收為基數，**本案無須觸及** |

### 0.4 一併查證確認的既有行為（供本案參考，非工項）

- 折扣碼的「使用上限」是**以報名筆數（隊伍）計**，不是人數；`team.codeConsumed` 保證每隊每碼只扣一次
- 消耗時機依付款方式不同：**PayUNI 付款成功才扣**、**銀行轉帳套用當下就扣**（既有產品決策）
- 檢查在建單、扣減在付款成功，中間隔著刷卡時間且無交易保護 → **`maxUses` 存在超賣可能**（名額設很小時才會顯形，記為觀察項，不在本案範圍）

---

## 1. 為什麼這兩案必須合併

兩案表面上是不同主題，實際上**共用同一個根因**：

> 系統用 `paymentStatus` 字串有沒有「已確認」三個字，來反推「這個人付款了沒」。

- A 案的問題：報名當下就說「報名成功」，沒付款也一樣 → 使用者被誤導
- B 案的問題：退費後字串被覆寫成「已退費 …」，不含「已確認」→ **被判成未付款**

兩案都在處理「付款狀態的語意」，且**改到同一批函式**。分開做會產生兩套判讀邏輯，日後必然發散。

### 1.1 重疊點清單（實際驗證過）

| 程式位置 | A 案要做 | B 案要做 | 處置 |
|---|---|---|---|
| `_resolveTeams:10012-10013` | 繳費提醒名單需同一判斷 | F6 排除已退費（🔴 EDM 不可逆） | **同批改，B 案版本為準** |
| `getAiInsights:11314` | C5a 加 `link` 深連結 | F4 payWait 排除已退費 | **同一個 `insights.push()`，同批改** |
| `getTodoList:11396` | — | F5 | B 案 |
| `createRegistrationPayment:7970` | — | F11 擋已退費 | B 案 |
| `confirmPayment:4859` | A8 補寄「已收到款項」信 | **未納管** ← 見 §2 M1 | **合併後新增閘門 + 寄信** |
| `updateTeamStatus:6754` | A7 補付款閘門、A6 信件分流 | — | A 案 |
| `registrations.html:504/:809` | C5b 付款篩選 chip | F9 F10 | **同檔，同批改** |
| `payments.html:321/:763` | — | F8 帳務數字、F16 退費對話框 | B 案 |
| 付款狀態判讀 | 逾期掃描需要 | §4 新增 `isRefunded()`／`isPaidConfirmed()` | **共用同一組純函式** |

---

## 2. 合併分析的新發現

### 🔴 M1 — `confirmPayment` 可以把「已退費」覆寫回「已確認」（兩份計畫都漏掉）

**這是合併分析才看得到的缺口。**

`functions/index.js:4859` `confirmPayment`（主辦方後台按「確認收款」）：

```js
// R5 H-9: 冪等 — 已確認的重複請求直接回成功
if (String(teamDoc.data().paymentStatus || "").includes("已確認")) {
  return { success: true, already: true };
}
await db.collection("teams").doc(teamId).update({ paymentStatus: "已確認 " + fmtNow() });
```

**只檢查「已確認」做冪等，完全沒有檢查「已退費」。** 已驗證：全段無 `已退費` 字樣。

**後果**：主辦方對一位已退費的報名者誤按「確認收款」→ `paymentStatus` 從
`"已退費 NT$1500 …"` 被**整串覆寫**成 `"已確認 …"` →

1. 退費紀錄在所有統計上**完全消失**（B 案要修的 `isRefunded()` 從此判不出來）
2. 帳務頁憑空多出一筆已收款
3. **且無法從 `paymentStatus` 還原**——原字串已被蓋掉

**為什麼兩份計畫都漏掉**：函式命名極具誤導性。系統裡有兩支名字相近、角色完全不同的函式：

| 函式 | 位置 | 實際角色 | 稽核日誌 |
|---|---|---|---|
| `confirmPayment` | `:4859` | **主辦方確認 ATM 收款**，寫 `paymentStatus:"已確認"` | 「確認付款」 |
| `confirmManualPayment` | `:7938` | **報名者端套折扣／算銀行轉帳應付金額**，只寫 `owedAmount`，**不碰 `paymentStatus`** | 「套用折扣(銀行轉帳)」 |

- **B 案 F12** 寫「`:7948 confirmManualPayment` 加已退費閘門」→ 位置正確、函式也該加閘門，但它**不是收款端點**；真正會覆寫 `paymentStatus` 的 `confirmPayment` 沒被納管
- **A 案 A8** 寫「`confirmManualPayment:4875` 補寄信」→ **函式名誤植**，正確是 `confirmPayment:4859`（當時 grep 到 `paymentStatus` 寫入行卻標錯所屬函式）

**兩案各錯一半，合併後才拼出完整圖像。**

### 🔴 M2 — 順序相依：繳費提醒若先上線，會對已退費者寄催繳信

A 案的提醒排程（B3）判斷「未繳費」的方式，與 B 案要修的 bug **完全相同**：

```
已退費者的 paymentStatus = "已退費 NT$1500 …"  → 不含「已確認」→ 判為未繳費
  ↓
被納入繳費提醒名單 → 收到「請於截止日前完成繳費，否則視同未報名成功」
  ↓
對一位剛完成退費的人催繳
```

差別在於：B 案的錯誤只是**顯示數字不對**（可回滾），A 案的錯誤是**主動寄信、不可撤回**。

**強制順序**：B 案的判讀函式與統計修正**必須先於** A 案的提醒排程上線。這是本合併案最重要的排序約束。

### 🟠 M3 — 逾期標記也必須排除已退費

A 案的 B2 逾期掃描原本排除「已取消」「退費申請中」（沿用 `checkAndReserveQuotaTx:208` 的模式），
但**沒有排除「已退費」**——因為 A 案當時不知道這個判讀陷阱。

若不修：已退費者會被標成「逾期未繳費」→ 主辦方在批次面板看到 →
**可能對一位已完成退費的人執行「取消報名」**（不可逆動作）。

### 🟠 M4 — A8 寄信依賴 M1 的閘門

A8 要在 `confirmPayment` 成功後寄「已收到款項，報名完成」信。
若 M1 的已退費閘門沒先做，對已退費者誤按確認 → **會寄出「報名已正式完成」給一位剛退費的人**。

**M1 必須與 A8 同批，且閘門在寄信之前。**

---

## 3. 合併後的完整工項清單

### 3.1 共用基礎（批 0）

| # | 位置 | 內容 |
|---|---|---|
| **S1** | `functions/index.js` 工具函式區 | `isRefunded(t)` / `isPaidConfirmed(t)`（B 案 §4 定義） |
| **S2** | `public/shared/` 新檔 | 同上前端版本，供 `payments.html`／`registrations.html`／`my.html` 共用 |
| **S3** | `_verify_merged/` | 原碼語意斷言測試（**從真實原始碼抽取**，不用同構複製 — 專案已知該模式零偵測力） |

```js
function isRefunded(t)      { return String(t && t.paymentStatus || "").includes("已退費"); }
function isPaidConfirmed(t) { return !isRefunded(t) && String(t && t.paymentStatus || "").includes("已確認"); }
```

### 3.2 統計讀取端（批 1 · B 案 W-STAT）

| # | 位置 | 端點 | 風險 |
|---|---|---|---|
| F1 | `index.js:4480` | `getDashboardStats` | 🟠 高（3 個前端依賴） |
| F2 | `index.js:4609` | `getRegistrationStats` | 🟡 中 |
| F3 | `index.js:6032` | `buildOrgStatsContext` | 🟢 低 |
| F4 | `index.js:11314` | `getAiInsights` | 🟡 中 |
| F5 | `index.js:11396` | `getTodoList` | 🟡 中 |
| F7 | `index.js:11650/11691/11732` | `listRegistrants` 家族 | 🟢 低 |

### 3.3 付款／收款閘門（批 2 · B 案 W-PAY ＋ 合併新增）

| # | 位置 | 內容 | 風險 |
|---|---|---|---|
| F11 | `index.js:7970` `createRegistrationPayment` | 擋已退費，**fail-open**（只在 `isRefunded` 為真時擋） | 🔴 極高（擋錯＝斷收入） |
| F12 | `index.js:7948` `confirmManualPayment` | 擋已退費 | 🟡 中 |
| **M1** | `index.js:4871` `confirmPayment` | **新增已退費閘門**（合併新發現） | 🟠 高 |
| A7 | `index.js:6754` `updateTeamStatus` | 補付款閘門（與 `acceptTeam:4920` 一致） | 🟡 中 |

### 3.4 通知內容正確化（批 3 · A 案 A 級）

| # | 位置 | 內容 | 風險 |
|---|---|---|---|
| **A5** | `index.js:3162-3202` | 通知全段 try/catch（**修既有根因 F：寄信失敗會讓報名回報失敗**） | 🟢 低（純防禦） |
| A1 | `index.js:3162-3195` | 確認信依「需付費 × 付款方式」分流 | 🔴 高（報名主路徑） |
| A3 | `index.js:7434-7455` | 付款成功信補「報名完成」 | 🟢 低 |
| A6 | `index.js:6785-6803` | 狀態更新信分流（不再一律綠色「正取」） | 🟡 中 |
| **A8** | `index.js:4859` `confirmPayment` | 補寄「已收到款項，報名完成」信（**依賴 M1**） | 🟡 中 |

### 3.5 後台前端（批 4）

| # | 位置 | 內容 |
|---|---|---|
| F8 | `payments.html:321` | 應收／待收／收款率整筆排除已退費 |
| F9 | `registrations.html:504` | 分析頁納管（§2.5：現在對是巧合） |
| F10 | `registrations.html:809` | 付款徽章三態（已收款／待付款／已退費） |
| C5b | `registrations.html` | 付款狀態篩選 chip（A 案） |
| A2 | `payuni-return.html:31-67` | 改查真實付款狀態（抄 `payment.html:440-475`） |
| A4-2 | `checkin.html:534-580` | 未繳費醒目橫幅 ＋ 應付金額（`owedAmount` 需 fallback） |

### 3.6 報名者端前端（批 5）

| # | 位置 | 內容 |
|---|---|---|
| F13 | `detail.html:1032/1043` | 已退費不顯示「前往付款」 |
| F14 | `payment.html:286` | 已退費隱藏付款方式 |
| F15 | `Manual.html:1095` | 更正敘述 |
| C1/C2 | `register.html` | 繳費規則條款、成功頁文案改「報名已受理」 |
| C3 | `my.html:461-463` | 未繳費倒數 |
| C4 | `detail.html:604` | 活動頁顯示繳費規則 |

### 3.7 退費意圖選擇（批 6 · B 案 W-D5）

| # | 位置 | 內容 | 風險 |
|---|---|---|---|
| F16 | `payments.html:763-779` | 退費對話框二擇一 | 🟠 高 |
| F17 | `firebase-bridge.js:186` | `ownerRefund` 加 `cancelRegistration` 位置參數 | 🟠 高（**必須同批佈**） |
| F18 | `index.js:4251/4178` | 後端參數；`hadRequest \|\| explicitCancel`；**未傳＝維持舊行為** | 🟠 高 |
| F19 | `i18n-admin-events-payments.js` | 改寫 `aePayRefundKeepNote`、新增二選項字串 | 🟢 低 |
| N5 | `_ownerRefundCore` | 補 `refreshRegistrantsForTeam` | 🟢 低 |
| A4-1 | `index.js:10802` `checkInTeamV2` | 已取消一律擋報到（**V2 才是現行路徑**） | 🟢 低 |

### 3.8 EDM（批 7 · 不可逆）

| # | 位置 | 內容 | 風險 |
|---|---|---|---|
| F6 | `index.js:10012-10013, 10027` | `_resolveTeams` unpaid 排除已退費 | 🔴 極高 |
| C5a | `index.js:11318-11323` | `getAiInsights` 加 `link` 深連結（與 F4 同段） | 🟢 低 |

### 3.9 繳費期限與自動提醒（批 8 · A 案 B 級，**最後做**）

| # | 位置 | 內容 | 風險 |
|---|---|---|---|
| B1 | `index.js:2409-2416`(`jk`)、`edit.html:228/1898/2004`、`index.js:2835` | `paymentDeadline` ＋ `blockUnpaidCheckin`（**四處，漏 `jk` 會靜默不存**） | 🟡 中 |
| B2 | 新函式 | 逾期標記（**排除已取消／退費申請中／已退費** ← M3） | 🟠 中高 |
| B3 | **新排程 `paymentJobs`**（`timeoutSeconds:540`） | 提醒排程；**不掛 `dailyJobs`**（其無 timeout，預設 60 秒） | 🔴 高（寄信不可逆 ＋ Gmail 配額） |
| B4 | `_runExpireStalePendingOrders` | 擴充涵蓋 `regPayments` | 🟢 低 |
| 建議3 | `registrations.html` | 逾期名單 CSV 匯出 | 🟢 低 |
| 建議4 | `paymentJobs` | 結構化統計 log | 🟢 低 |

---

## 4. 合併後的施工批次

| 批 | 內容 | 部署 | 驗收門檻 |
|---|---|---|---|
| **0** | S1 S2 S3 共用函式 ＋ 測試 | 不部署 | 原碼斷言通過 |
| **1** | F1 F2 F3 F4 F5 F7（統計讀取，6 支） | functions 指名佈 | `CZXJ323` 待付款＝18、長青組＝9 |
| **2** | F11 F12 **M1** A7（閘門，4 支） | functions 指名佈 | **未付款者仍可付款**（最重要回歸）；已退費被擋 |
| **3** | **A5 先**、A1 A3 A6 A8（通知，依賴批 2） | functions 指名佈 | 三路徑信件內容正確；報名不受影響 |
| **4** | F8 F9 F10 C5b A2 A4-2（後台前端） | Hosting 預覽 → 驗收 → clone | 帳務三數字；付款結果頁不再假成功 |
| **5** | F13 F14 F15 C1 C2 C3 C4（報名者端） | Hosting 預覽 → 驗收 → clone | 已退費被擋；未付款可付 |
| **6** | F16 F17 F18 F19 N5 A4-1（退費意圖 ＋ 報到閘門） | **後端＋bridge 同批**；Hosting 預覽 | 測試活動驗二選項；退到正確金額 |
| **7** | F6 C5a（EDM，不可逆） | functions 指名佈 ＋ **補償控制四項** | 三種篩選筆數正確 |
| **8** | B1 B2 B3 B4 建議3 建議4（期限與排程） | functions ＋ Hosting；**dry-run 先行** | dry-run 名單正確後才開真寄 |

### 4.1 排序的三條硬約束

1. **批 0 → 全部**：判讀函式是所有批次的基礎
2. **批 1/2 → 批 8**（M2）：統計判讀修好之前，**絕不可**啟用會主動寄信的提醒排程
3. **批 2（M1）→ 批 3（A8）**：閘門先於寄信，否則會對已退費者寄「報名完成」

### 4.2 為什麼不能「一次全佈」

| 不可逆動作 | 所在批 | 若無驗收窗口 |
|---|---|---|
| EDM 寄信 | 7 | 寄錯人收不回；cron 會自動觸發 |
| 繳費提醒寄信 | 8 | 同上，且可能打爆 Gmail 500/天配額 → **連報名確認信一起停擺** |
| 退刷金額 | 6 | 位置參數錯位＝退錯金額，真實金流 |
| 批次取消報名 | 8 | 釋出名額、可能已被他人遞補 |

**這四項是「一次全佈」唯一真正的代價。** 其餘批次即使出錯，回滾後零殘留。

---

## 5. 合併後的產品口徑（兩案裁決彙整）

| # | 題目 | 裁決 | 來源 |
|---|---|---|---|
| D1 | 退費後算不算報名 | 維持「退出才減、退押金不減」 | B 案 |
| D2 | 應收總額 | 整筆排除 | B 案 |
| D3 | 部分退費 | 視同全退，保留款另案 | B 案 |
| D4 | 已退費者付款入口 | 前後端都擋 | B 案 |
| D5 | 退費意圖 | 對話框二擇一；取消報名但**資料保留不刪除** | B 案 |
| D6 | 修法 | 唯讀派生、零資料遷移 | B 案 |
| D9 | EDM 節奏 | 一次改完（已具名接受風險） | B 案 |
| — | 逾期處置 | **標記後由主辦方批次確認，永不自動取消** | A 案 |
| — | 繳費期限 | 預設＝報名截止日，可改成截止後 N 天；既有活動留空＝不啟用 | A 案 |
| — | 報到 QR | **照常發放**，報到時提示未繳費可當面收款 | A 案 |
| — | 報到硬擋 | 不硬擋；做成活動層級開關（預設關）；**已取消一律擋** | A 案 |
| — | 回溯 | 系統不自動補發；用既有 EDM 由主辦方自主寄 | A 案 |

---

## 6. 合併後的風險評估

### 6.1 最高風險三項（合併後不變，但互相牽動）

| 風險 | 所在 | 最壞後果 | 殘留損害 |
|---|---|---|---|
| `createRegistrationPayment` 閘門寫反 | 批 2 F11 | **擋住正常未付款者付款＝直接斷收入** | 可回滾，期間流失的付款追不回 |
| `submitRegistration` 改壞 | 批 3 A1 | **所有新報名失敗**，但資料已寫入 → 重複報名 | **需人工清理重複資料與名額** |
| 寄信類（EDM／提醒） | 批 7、8 | 寄錯人、或打爆 Gmail 配額連帶報名確認信停擺 24h | **信收不回** |

### 6.2 合併帶來的**新增**風險

| 風險 | 說明 | 緩解 |
|---|---|---|
| **改動面擴大**（單一 `index.js` 約 20 處） | 多段替換同檔易出錯 | 每批後 `git diff` 逐行對照本文件；清單外異動視為越界 |
| **批次數增加至 9** | 部署次數多 → 429 配額風險上升 | 每批 ≤ 6 支；逐支核 `updateTime`；**批間留間隔** |
| **前後端口徑不同步窗口變長** | 批 1 後端先佈、批 4 前端才佈 | 後端先佈安全（舊前端只是「數字變對」）；**F17 為唯一必須同批的例外** |

### 6.3 合併帶來的**降低**風險

| 項目 | 說明 |
|---|---|
| **M1 被發現** | 若分開做，這個「已退費被覆寫回已確認」的缺口會一直存在，且會讓 B 案的修正在特定情境下失效 |
| **M2 被攔截** | 若 A 案先上線，會對已退費者寄催繳信——合併後被排序約束擋下 |
| **判讀邏輯統一** | 一組純函式，不會出現兩套發散的判斷 |
| **A5 先行** | 批 3 先上 try/catch 安全網，再改信件內容 |

### 6.4 具名接受的風險（沿用 B 案 §6.4）

> **D9(b)：EDM 一次改完。** `sendCampaignNow` 與 cron `processScheduledCampaigns`
> 會在無人工驗收窗口下直接以新邏輯寄信，已寄出的信無法撤回。
> **補償控制為必做**：① 部署窗口先清空 `scheduled` 公告 ② 部署後立即以
> `getCampaignRecipients` 核對三種篩選筆數 ③ 測試寄送 ④ 留守 30 分鐘。

---

## 7. 還原點與回滾

- 施工前 tag：`restore-20260922-pre-merged`（**指向 `f2b21ce`**，非原訂的 `18f641f`——見 §0）
- 前端備份：`_backup_20260922_merged/`
- 回滾：Hosting 走 `hosting:clone`；functions 以 tag checkout 後**指名重佈該批**
- **批 6（金流）與批 8（寄信）另備**：執行前先在測試活動驗證，不得直接用正式活動

---

## 8. 兩案原文件的處置

| 文件 | 處置 |
|---|---|
| `PLAN_2026-09-09_unpaid-registration-ux.md` | **保留**（含五輪複掃的完整根因分析與 27 項發現），本文件引用之 |
| `PLAN_2026-09-22_refund-stats-consistency.md` | **保留**（含線上唯讀查證數字與裁決紀錄），本文件引用之 |
| 本文件 | **施工依據**。兩案的工項編號（F1–F19、A1–A8、B1–B4、M1–M4）在此統一 |

**需回頭更正的兩處**（已在 §2 M1 說明）：
- A 案 §15：A8 的位置由 `confirmManualPayment:4875` 更正為 `confirmPayment:4859`
- B 案 §3.2：F12 保留（`confirmManualPayment` 確實該加閘門），但**新增 M1** 納管真正的收款端點

---

## 9. 待授權

本文件為**合併後的完整修正案**，範圍已一次定義完畢，不再追加。
**本案本體尚未開工、未部署。** 開始實作需明確授權；授權後依 §4 批次推進，
每批通過驗收才進下一批。§4.1 的三條排序約束不得調換。

### 9.1 開工前的必做清單（因 §0 基準變動而新增）

1. 打新的還原點 tag，指向當時的 HEAD（不是 `18f641f`）
2. **重新核對所有行號**——兩份來源計畫的行號基準是 `18f641f`，`register.html` 已位移約 100 行
3. 以「函式名稱」而非行號定位每一個 F／A／M 工項，逐項確認仍存在且語意未變
4. 特別確認 §3.3 的 **F11 `createRegistrationPayment` 仍缺付款狀態閘門**（0923 那批未處理）

### 9.2 施工紀錄

| 日期 | 事件 | commit |
|---|---|---|
| 2026-09-22 | 兩案合併分析完成、發現 M1（`confirmPayment` 可覆寫已退費） | — |
| 2026-09-23 | **折扣碼修正獨立上線**（不屬本案，但變動基準） | `f2b21ce` |
| — | 本合併案待授權開工 | — |
