# RegMaster-V3 上線前「真人使用者」UX 全功能測試 — Cowork 指派提示詞

> 把以下整份內容貼給 Cowork / 瀏覽器代理執行。目標：**像真實使用者一樣，從前端逐一操作每個身分與流程**，記錄每一個不順、卡住、看不懂、功能不完善之處，並提出改善建議。**不要只看程式碼**——務必實際點擊、輸入、等待、觀察畫面。不限時間與 token，越徹底越好。

---

## 0. 你的角色與方法
你是一位嚴格、細心的 **QA / UX 測試員**，第一次接觸這個產品。請用**瀏覽器**（claude-in-chrome 或 computer-use）實際操作：
- 用「新使用者」的心態：看得懂嗎？下一步該按哪？錯誤訊息清楚嗎？等待有提示嗎？
- 每個流程**走到底**（不是點開就算）。例如「建立活動」要真的填完表單、存檔、開啟報名、看到結果。
- 桌機與**手機尺寸**都要測（用瀏覽器縮放/裝置模擬）。
- 切換**中文 / English**（右上角語言鈕）各看一次關鍵頁面，找漏翻譯或排版跑掉。
- 每發現一個問題，**截圖** + 記錄「在哪一頁、做了什麼、預期 vs 實際」。

### ⚠️ 安全限制（務必遵守）
- 這是測試站，可自由建立/修改/刪除資料。
- **不要輸入任何真實信用卡、銀行帳號、個資**。金流頁面若需付款，僅測到「導向 PayUNI / 顯示 ATM 虛擬帳號」這一步即可，不要完成真實付款。
- 不要更改 `qa_sys` 以外帳號的系統設定中「會影響全站」的危險項（例如 production reset、清空 audit log 請只觀察不要真的執行）。

---

## 1. 測試環境與帳號
- 網址：**https://regmaster-v3.web.app**
- 主辦方 / 管理登入頁：`/login.html`
- 所有測試帳號密碼皆為：**`QAtest2026!`**

| 帳號 (username) | 角色 | 用途 |
|---|---|---|
| `qa_free` | 主辦方（Free 方案） | 測免費方案的限制與 gating |
| `qa_starter` | 主辦方（Starter 方案） | 測 Starter 解鎖的功能 |
| `qa_pro` | 主辦方（Pro 方案） | 測 Pro 完整功能（含 AI、證書、評分） |
| `qa_team` | 主辦方（Team 方案，也是團隊擁有者） | 測 Team 無限額度 + 成員協作 |
| `qa_sys` | 系統管理員 | 測 `/admin/system.html`（方案設定、帳務結算、使用者分析、稽核日誌、Gemini 金鑰） |
| `qa_manager` | Team 成員：管理者 | 以成員身分管理 `qa_team` 的活動（view/checkin/scoring/manage） |
| `qa_judge` | Team 成員：評審 | 只應能評分（scoring） |
| `qa_staff` | Team 成員：工作人員 | 只應能報到（checkin） |
| （報名者） | 公眾報名者 | **無預設帳號**：請在測試中用公開報名頁**實際報名一隊**，自行設定報名密碼，之後用「報名編號＋密碼」於 `/my.html` 或活動頁登入查詢 |

> 提示：`qa_manager/qa_judge/qa_staff` 用各自帳號登入後，應能看到並進入 `qa_team` 建立的活動；請驗證他們**只能**做被授權的事（評審不應能改設定、工作人員不應能評分）。

---

## 2. 測試範圍（請逐項打勾並記錄）

### A. 公開 / 未登入（匿名訪客）
1. 首頁 `index.html`、`pricing.html`、`features.html`、`about.html`、`contact.html`：閱讀順暢度、CTA 是否清楚、「14 天免費試用」訊息是否到位、手機排版。
2. 聯絡表單 `contact.html`：實際送出一筆，確認有成功提示。
3. 活動探索頁 `/events/`：搜尋、分類篩選、卡片資訊、點進活動。
4. 活動詳情頁：資訊完整度、倒數、規章 PDF 下載（**務必按下「下載規章」確認真的打開 PDF**）、海報、AI 問答（右下角小幫手，問幾個問題看回答品質與「打字中」動畫）。
5. **社群分享**：點「分享活動 / 複製連結 / LINE / Facebook」，把產生的 `/e/<id>` 連結貼到聊天視窗或用工具檢視，確認**有標題＋描述＋海報縮圖預覽**。
6. 註冊流程 `signup.html`：填表 → 取得 OTP。**注意**：OTP 寄到 email，若你沒有該信箱可只測到「送出後要求輸入驗證碼」這步，並測「重新寄送驗證碼」按鈕有回應。記錄整體註冊體驗。

### B. 主辦方（對 `qa_free`/`qa_starter`/`qa_pro`/`qa_team` 各做一輪，重點比較方案差異）
1. 登入 → **Onboarding** 引導（注意：非 Team 方案**不應**出現「邀請夥伴」步驟）。
2. **儀表板** `/admin/`：KPI 數字、報名趨勢圖 + 7/30/90 切換、迷你走勢圖、待辦清單、AI 洞察。
3. **建立活動**：走完整流程（基本資料、組別、名額、收費、付款方式、日期、開放報名）。Free 方案請故意超過名額上限，確認有正確阻擋與升級提示。
4. **表單設計器** form-builder：拖放欄位、特殊欄位、**條件邏輯**（設一個「選 A 才顯示 B」），存檔後到報名頁確認真的會顯示/隱藏。「預覽 as registrant」。
5. **海報 / 規章上傳**：上傳圖片與 PDF；試 **AI 海報萃色**、**AI 讀規章產生評分標準**（Pro 才有）。
6. **報名管理** registrations：
   - 統計頁兩個子分頁「**分類統計**」「**時間趨勢**」切換是否正常、圖表是否都有資料、**查詢區間下拉**（今日/3/7/14/30/60/90/自訂）是否即時更新時間圖。
   - 報名列表搜尋、隊伍詳情、寄送 email、CSV/Excel 匯出（Free 應被鎖）、退費管理。
7. **公告 / EDM** announcements：新增範本、插入**變數**（{{競賽名稱}}等，看編輯區下方提示）、預覽、**立即發送**、**排程發送**（選未來時間，確認狀態變「已排程」）、寄出後看**開信/點擊統計**。
8. **報到** checkin：設定（時間窗、GPS 地圖選點、解鎖 PIN——確認 PIN 欄位有「僅前端鎖」說明）、名冊搜尋、啟動**自助報到機 Kiosk** 模式看畫面。
9. **證書 / 名牌** certificates（Pro+）：用設計器做一張、放變數、批次產生、自助下載設定。
10. **評分** scoring（Pro+）：建立評分表（rubric）、評審面板、公平性設定。
11. **AI 助理** `/admin/ai.html`：點四張建議卡片（應立即開新對話送出 + 打字動畫）、手動問答、對話歷史。
12. **設定** settings：個人資料、收款帳戶、**通知偏好開關**、**2FA 啟用流程**、**登入裝置 →「登出所有裝置」**、（Team）**成員與權限**邀請。
13. **方案 / 授權** license：查看目前方案、升級報價試算。
14. **方案 gating 差異**：明確記錄每個方案哪些功能被鎖、升級提示是否清楚（對照 pricing 頁宣稱）。

### C. Team 成員協作（先用 `qa_team` 建好一個活動，並在評分設定中指派 `qa_judge` 為評審）
1. `qa_manager` 登入：應能進入 qa_team 的活動並管理。
2. `qa_judge` 登入：**只應**看到/做評分；嘗試進設定/刪除應被擋。實際對幾隊評分，再回 qa_team 看「評分結果」與兩張盒鬚圖（含**各組別評審間評分分布**）。
3. `qa_staff` 登入：**只應**能報到；嘗試評分/改設定應被擋。

### D. 系統管理員 `qa_sys`（`/admin/system.html`）
1. 方案設定（價格/手續費/額度/功能矩陣）讀取與儲存。
2. 帳務管理 / 結算（訂單、毛/淨額、結算並匯款——只觀察）。
3. 使用者分析（流量/裝置/方案/MRR/漏斗/熱門活動等 9 張圖、日期區間）。
4. 稽核日誌、Gemini 金鑰設定（不要刪）。

### E. 報名者（公眾）全流程
1. 用 `qa_pro` 或 `qa_team` 建立的公開活動，**實際報名一隊**（填完整表單、上傳檔案、選付款方式）。
2. 金流：測到導向 PayUNI / 顯示 ATM 虛擬帳號即可（**不要真的付款**）。
3. 報名確認信、`/my.html` 用 email/電話查詢、修改報名、退費申請。
4. （若主辦方開啟）自助報到、證書自助下載。

### F. 跨頁共通
- 中／英切換、手機 RWD、載入中與空狀態提示、錯誤訊息友善度、按鈕 disabled/loading 狀態、返回上一頁是否正常。

---

## 3. 產出格式（請最後彙整成一份報告）
針對每個發現，用下列格式條列：

```
[區域/頁面] [嚴重度] 標題
- 身分：哪個帳號
- 重現步驟：1) ... 2) ... 3) ...
- 預期： / 實際：
- 截圖：（附上）
- 建議：
```
嚴重度：🔴 阻斷（功能壞/無法完成）｜🟠 影響體驗（卡頓/困惑/醜）｜🟡 小瑕疵｜💡 功能建議。

最後附上：
- **總體 UX 評分（1–10）** 與一句話總結。
- **上線前必修清單**（🔴/🟠）。
- **建議優先做的 5 個改善**。

---

## 4. 測試結束後（清理，交回給開發者執行）
測試會產生 qa_* 帳號建立的測試活動與報名資料。開發者會以下列方式一鍵清除（你不需執行，列此備查）：
- 呼叫 `qaSeed`（secret 保護）`action:"cleanup"` → 刪除所有 `_qatest` 帳號/授權/成員，以及 qa_* 帳號建立的活動與其報名/評分/付款等附屬資料。
- 之後移除暫時的 `qaSeed` 函式。
