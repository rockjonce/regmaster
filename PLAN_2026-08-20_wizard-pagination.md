# 批 3 設計稿：報名精靈「一區塊一頁」重構

狀態：**設計已核定（2026-08-20，四項決策點已拍板）**，待排程實作；實作走預覽頻道 UAT。
關聯：表單設計五項問題之第 3 項（區塊順序無效）與第 5 項（一區塊一頁）。本設計同時根治兩者。

---

## 1. 目標與非目標

**目標**
- 表單設計器的「一個區塊」＝報名者看到的「一頁」；頁面順序＝設計器區塊順序（跨角色也生效）。
- 上方進度條顯示 1…N，當前頁反白。
- schema 零遷移：任何既有活動不改資料即得新版面。

**非目標（本批不做）**
- 不改表單設計器 UI（它已是區塊模型）。
- 不改欄位型別/驗證規則/付款流程。
- 不做「頁間條件跳轉」（如答 A 跳頁 5）。

## 2. 頁面結構

```
頁 1（固定）   組別 / 梯次 / 隊名          ＝現行步驟 1，不對應任何區塊
頁 2..N+1      schema.sections 依序，一區塊一頁（student / teacher / custom 混排均可）
頁 N+2（固定） 確認資料 + 付款 + 送出       ＝現行步驟 4
```

- 總頁數 = 區塊數 + 2。
- student/teacher 區塊頁＝該成員的一張卡（現行 member-card 內容原樣搬進頁）。
- custom 區塊頁＝該區塊欄位（沿用批 2 的區塊標題＋說明呈現）。
- 免費活動的最末頁維持現行「無付款區」變體。

## 3. 進度條設計

**桌機（不設頁數上限的自動退化——決策 3）**：數字圓點＋連接線
```
 ●━━━●━━━◉━━━○━━━○
 1    2    3    4    5
```
- 已完成：實心（--ink 底、白字、✓ 可選）；可點擊回跳（僅能回到已完成頁）。
- 當前：--acc 底白字、外圈 ring（既有 --ring token）＝「反白顯示」。
- 未到：--surface-2 底、--muted 字；不可點。
- 圓點下方小字顯示區塊標題（超過 6 字截斷；行動版隱藏文字只留數字）。

**頁數很多時（桌機）**：不自動退化為細條（決策 3：由活動管理者自行決定區塊數）。圓點列改為
可水平捲動、圓點縮小（28px→22px），當前圓點自動捲入視野置中；標題文字在圓點過密時隱藏、
僅當前頁顯示標題。
**設計器端提醒（決策 3 配套）**：表單設計器儲存時若區塊數 > 8，彈出一次性提示
「本表單將分為 N+2 頁，報名者需按 N+1 次下一步；確定要此頁數嗎？」——僅提醒不阻擋。
**行動版（≤640px）**：一律細進度條＋「3 / 12 ‧ 標題」；沿用現行 footer 的「步驟 x / y」位置。

## 4. 關鍵行為規則

| 情境 | 規則 |
|---|---|
| 區塊數 = 0（純 legacy 活動無 schema） | 維持現行 3-4 步精靈不變（fallback 路徑保留） |
| 區塊數 = 1 | 仍走新架構：3 頁（組別→區塊→確認）。與現行體驗幾乎等價 |
| 驗證時機 | 「下一頁」時驗當頁欄位（沿用 validateStep 的欄位級規則）；最末頁送出前全量複驗 |
| 條件欄位（logic.key）參照他頁欄位 | 允許；被控欄位所在頁進入時重算顯隱。控制欄位改值後回跳，已填的被隱藏欄位值照現行規則不送出 |
| 編輯回填（?edit=）| 全部頁預填；進度條全部標記可點，使用者可任意跳頁修改 |
| 付款失敗返回 | 直接回最末頁（現行行為不變） |
| 上一步 | 永遠可用（第 1 頁除外） |
| 語言切換 | 進度條標題與頁面 chrome 隨 lang-changed 重繪（沿用本輪建立的重繪模式＋一次性綁定旗標） |

## 5. 技術改動範圍（估）

| 檔案 | 內容 | 估行數 |
|---|---|---|
| events/register.html | pane 由固定 4 個改為動態產生；showStep/validateStep/進度 UI 重寫；步驟標題來源改區塊 | ~280-350 |
| shared/i18n-events-register.js | 進度條相關鍵 ~6 個（中英） | ~12 |
| functions | **零改動**（schema 不變、提交格式不變） | 0 |

風險等級：**高**（報名主流程）。緩解：預覽頻道完整 UAT 後才上正式站；還原點＋單檔回滾。

## 6. UAT 清單（預覽頻道上要全過）

1. 新報名：多區塊（student×2+teacher+custom×2）逐頁填寫送出成功
2. 區塊順序在設計器調動 → 報名頁頁序跟著變（含 custom 插在成員前）
3. 必填擋下一頁；錯誤訊息定位正確
4. 條件欄位跨頁顯隱
5. ?edit= 回填、跳頁修改、重送
6. 免費活動（無付款）與付款活動最末頁
7. 行動版進度條、>8 區塊退化模式
8. 中英切換（進度條、按鈕、標題）
9. legacy 無 schema 活動走 fallback 不變
10. 送出資料與現行版位元組級一致（對照組活動）

## 7. 決策點（2026-08-20 已拍板）

- [x] 進度條圓點下顯示區塊標題（桌機）：**要**
- [x] 已完成頁可點擊回跳：**可**
- [x] 頁數退化門檻：**不自動退化，由活動管理者決定**；設計器在區塊 > 8 時儲存前彈提示提醒（不阻擋）。進度條以縮小圓點＋水平捲動容納多頁
- [x] 區塊數 = 1：**仍走新架構**，心智模型一致
